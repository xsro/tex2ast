# Default remove-changes configuration
# Lines with {old} will have their content removed
# Lines with {new} will have their content kept
# Lines with both {new}{old} will replace old with new
# Comments start with #

\cancel{old}
\xcancel{old}
\sG{old}
\cG{old}
\tG{new}
\replaceG{new}{old}